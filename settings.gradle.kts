rootProject.name = "wds-telegram-bot-lib"

// Демонстрационный подпроект: тестовый бот, который зависит от библиотеки
// (корневой проект) и показывает её возможности — команды, callback-кнопки,
// клавиатуры, цепочку обработчиков. Это «обёртка» над библиотекой для ручной
// проверки; сама библиотека остаётся в корне.
include("example-bot")
